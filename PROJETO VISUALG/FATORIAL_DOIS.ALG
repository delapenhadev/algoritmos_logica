Algoritmo "fatorial"

Var
   fat,n:inteiro

Inicio
   escreva("Digite o valor de N: ")
   leia(n)

   fat <- 1
   enquanto n >= 1 faca
       fat <- fat * n
       n <- n-1
       escreval(fat)
   fimenquanto

Fimalgoritmo